name: Deploy WhatsApp Assistant

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ubuntu
          key: ${{ secrets.DEPLOY }}
          port: 22
          script_stop: true
          script: |
            # 1. Load fnm and Node into the path
            export PATH="$HOME/.local/share/fnm:$PATH"
            eval "$(fnm env --shell bash)"

            cd /var/www/whatsapp-assistant

            # 2. Stop the service (or use PM2 if you switched)
            sudo systemctl stop whatsapp-assistant

            # 3. Update code
            git pull origin main

            # 4. Install dependencies and Build using Node
            export NODE_ENV=production
            
            # Build tooling (tsc, vite plugins) lives in devDependencies.
            # Install with dev deps for build, then prune after build.
            npm install --include=dev

            npm run db:migrate
            npm run db:push
            npm run build

            # Keep production runtime light after assets are built.
            npm prune --omit=dev

            # 5. Restart the service
            sudo systemctl restart whatsapp-assistant